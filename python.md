## Create vitual enviroment python:
```
pip install pew
pew new bt1
pew in bt1
```

```
pip install virtualenv
virtualenv -p python3 pymienv
source pymienv/bin/activate   # active vitual inviroment
pip install requests ipython
```
